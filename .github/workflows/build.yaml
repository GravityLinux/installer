name: build

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  workflow_call:
    outputs:
      installer_ver:
        description: Gravity Linux installer built version string
        value: ${{ jobs.build.outputs.installer_ver }}

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      installer_ver: ${{ steps.compile.outputs.installer_ver }}

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          fetch-tags: true
          submodules: recursive

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y gcc-aarch64-linux-gnu clang lld llvm
          sudo apt-get install --no-install-recommends -y libarchive-tools 7zip jq imagemagick
          rustup target install aarch64-unknown-none-softfloat

      - name: Build Gravity Linux installer
        id: compile
        run: |
          ./build.sh
          echo "installer_ver=$(cat releases/latest).tar.gz" >> $GITHUB_OUTPUT

      - name: Upload artefact
        uses: actions/upload-artifact@v7
        with:
          name: installer-build
          path: |
            releases/*
